spring:
  datasource:
    url: jdbc:mysql://localhost:3306/curso?createDatabaseIfNotExist=true
    username: iuri
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: create # Você pode ajustar conforme necessário: validate | update | create | create-drop
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect